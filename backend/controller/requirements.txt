# Controller Python — Microcredenciales Blockchain (Besu + OpenID4VCI)
fastapi>=0.138.0
uvicorn[standard]>=0.49.0
pydantic[email]>=2.13.0
httpx>=0.28.1
python-multipart
websockets<16.0.0

# Criptografía (firma de credenciales W3C / OpenID4VC)
cryptography
ecdsa
grpcio
protobuf

# Utilidades
qrcode[pil]
python-jose[cryptography]
python-dotenv
requests
aiofiles

# OpenID4VC dependencies (compatibilidad con wallets Lissi/WaltID/DIDRoom/EUDI)
PyJWT>=2.8.0
jwcrypto>=1.5.0
base58>=2.1.1

# Logging y monitoreo
structlog
colorama

# Base de datos
sqlalchemy
alembic
psycopg2-binary

# Autenticación portal
bcrypt>=4.0.0

# Blockchain EVM (Smart Contracts)
web3>=6.11.0
